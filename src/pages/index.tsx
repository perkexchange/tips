import { FC } from 'react';
import Layout from '@/components/Layout';
import Hero from '@/components/Hero';

const Home: FC = () => {
  return (
    <Layout>
      <Hero />
    </Layout>
  );
};

export default Home; 